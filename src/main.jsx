import React from 'react';
import ReactDOM from 'react-dom/client';
import CBPSimulation from './CBPSimulation';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CBPSimulation />
  </React.StrictMode>
);
